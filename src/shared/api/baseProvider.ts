/**
 * 基础提供者接口和抽象类 —— 兼容垫片
 *
 * 定义已迁移到统一承载处 `src/shared/ai/core/BaseProvider.ts`（Phase 3c）。
 * 本文件保留 re-export 以兼容现有 import，行为不变。
 */
export type { BaseProvider } from '../ai/core/BaseProvider';
export { AbstractProviderCore, AbstractBaseProvider } from '../ai/core/BaseProvider';
